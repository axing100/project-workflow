interface:
  display_name: "Project Workflow"
  short_description: "Plan and execute complex projects with approval gates"
  default_prompt: "Use $index to plan and execute this complex project with a confirmation gate."
policy:
  allow_implicit_invocation: true
