interface:
  display_name: "Project Workflow"
  short_description: "Approve plans and schedule native agents safely"
  default_prompt: "Use $index to plan this complex project, wait for approval, then actively schedule safe native Codex agents."
policy:
  allow_implicit_invocation: true
