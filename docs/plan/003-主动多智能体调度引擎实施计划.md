---
workflow: "project-workflow/v1"
plan_id: "project-workflow-active-multi-agent-engine"
revision: 1
phase: "COMPLETED"
approved_revision: 1
approved_at: "2026-08-20T07:57:31+00:00"
confirmation_record: "好，开始改造"
---

# Project Workflow 主动多智能体调度引擎实施计划

- 设计文档：[003-主动多智能体调度引擎设计](../design/003-主动多智能体调度引擎设计.md)
- 执行方式：本次引擎实现采用单智能体串行开发；完成后插件支持计划驱动的主动多智能体调度。
- 当前基线：7 项状态机测试、三个技能 validator 和插件 validator 全部通过。
- 已有工作树修改：保留上一轮已确认的 `plugins/project-workflow/skills/index/SKILL.md` 准入规则修改。
- 提交授权：已授权；确认原文为 `帮我提交并推送到远端`。
- 推送授权：已授权；目标为当前 `origin/main`。

## T01 定义持久化调度协议

- 状态：[x]
- 目标：确定执行模式、任务 schema、授权语义、状态迁移和运行时降级规则。
- 范围：`references/workflow-protocol.md`、`references/multi-agent-orchestration.md`、`references/execution-checklists.md`。
- 范围外：Python 实现和技能执行逻辑。
- 前置条件：本计划 revision 1 已确认。
- 风险：协议字段与后续脚本或技能不一致。
- 验收标准：
  - [x] 定义 AUTO、SINGLE、MANUAL 三种模式。
  - [x] 定义 companion JSON、任务状态和合法转换。
  - [x] 明确计划确认只授权范围内委派，不推导其他权限。
  - [x] 明确运行时不可用、冲突、失败、恢复和重新确认路径。
- 验证：
  - [x] 人工交叉检查三份参考文档字段和术语一致。
- 证据：三份参考文档统一使用 AUTO_MULTI_AGENT、SINGLE_AGENT、MANUAL_MULTI_AGENT、companion state 和 bounded retry 术语。

## T02 实现调度状态脚本及测试

- 状态：[x]
- 目标：以确定性脚本管理任务 DAG、分配、冲突、完成、释放和阻塞状态。
- 范围：新增 `scripts/orchestration_state.py`、新增 `tests/test_orchestration_state.py`。
- 范围外：调用运行时子智能体工具。
- 前置条件：T01。
- 风险：循环依赖、写入范围重叠、原子写入或恢复校验遗漏。
- 验收标准：
  - [x] 支持 `validate`、`ready`、`assign`、`complete`、`release`、`block`、`inspect`。
  - [x] 拒绝未知依赖、循环依赖、非法状态转换和冲突分配。
  - [x] plan ID/revision 与计划不一致时拒绝执行。
  - [x] 所有写入为原子替换且仅依赖 Python 标准库。
- 验证：
  - [x] `python3 -m unittest discover -s plugins/project-workflow/tests -v` — 新旧测试全部通过。
  - [x] CLI 场景验证两项并行、冲突排除、失败释放和完成后解锁依赖。
- 证据：19 项测试通过；覆盖安全 wave、嵌套路径冲突、DAG 循环、revision、AUTO/MANUAL/SINGLE、协调者分配、max_attempts、release/block 和依赖解锁。

## T03 接入规划和主动执行调度

- 状态：[x]
- 目标：让 plan 生成调度记录，让 execute 主动创建、跟进和验收 Worker。
- 范围：`skills/plan/SKILL.md`、`skills/execute/SKILL.md`，必要时小幅调整 `skills/index/SKILL.md`。
- 范围外：实现 Codex 运行时工具或绕过运行时权限。
- 前置条件：T01、T02。
- 风险：协调者和 Worker 职责混淆、过度并行或失败后重复工作。
- 验收标准：
  - [x] plan 明确选择模式、拓扑、Worker 上限、DAG、写入范围和 Agent eligibility。
  - [x] AUTO 模式在计划批准且运行时允许时主动调度。
  - [x] Worker 数不超过计划、运行时和 ready task 三者上限。
  - [x] 协调者独占计划状态、Git、架构决策、最终验收和交付。
  - [x] 委派失败按补充一次、释放/接管、阻塞的顺序处理。
  - [x] 重大变更中断受影响调度并回到重新确认。
- 验证：
  - [x] plan、execute 两个已修改技能的 `quick_validate.py` 命令通过；三个技能将在 T05 再做全量验证。
  - [x] 使用静态场景逐项核对 AUTO、SINGLE、无工具降级和重大变更路径。
- 证据：plan 持久化模式、拓扑、scope 与 companion state；execute 使用原生 Codex agent 的 assign/spawn/wait/verify/retry 循环，并规定 coordinator fallback 与重新确认边界。

## T04 更新公开文档和插件版本

- 状态：[x]
- 目标：让用户理解自动多智能体模式的使用流程、覆盖方式和限制。
- 范围：`README.md`、`README.zh-CN.md`、`.codex-plugin/plugin.json`。
- 范围外：GitHub Release、Tag 和远程发布。
- 前置条件：T03。
- 风险：中英文描述、命令和实现不一致。
- 验收标准：
  - [x] 两份 README 增加执行模式、用户流程、降级和权限说明。
  - [x] manifest 版本升级为 `0.3.0`，描述反映主动调度能力。
  - [x] 文档明确插件不能创造运行时不存在的子智能体能力。
- 验证：
  - [x] `python3 -m json.tool plugins/project-workflow/.codex-plugin/plugin.json` — 通过。
  - [x] 中英文关键术语和示例命令交叉检查一致。
- 证据：README 中英文均说明三种模式、原生 UI、fallback、权限边界与 orchestration_state 命令；manifest 和入口元数据已更新。

## T05 全量验证、本机重装与交付

- 状态：[x]
- 目标：验证调度状态、技能、插件和本机缓存，形成交付证据。
- 范围：测试、validators、本机 cachebuster 重装、源码 manifest 恢复、交付报告和计划状态。
- 范围外：Git 提交和 GitHub 推送。
- 前置条件：T02、T03、T04。
- 风险：本机缓存未更新、cachebuster 进入源码或测试只覆盖正常路径。
- 验收标准：
  - [x] 新旧单元测试全部通过。
  - [x] 三个技能和插件 validator 全部通过。
  - [x] 插件安装状态为 installed、enabled。
  - [x] 安装缓存包含新调度规则，源码 manifest 恢复为 `0.3.0`。
  - [x] Git 只包含本计划预期修改且没有提交或推送。
  - [x] 交付报告记录测试、安装、限制、回滚和未授权远程操作。
- 验证：
  - [x] `python3 -m unittest discover -s plugins/project-workflow/tests -v`。
  - [x] 三个 `quick_validate.py`。
  - [x] `validate_plugin.py plugins/project-workflow`。
  - [x] `codex plugin list --json`。
  - [x] `git diff --check` 与 `git status --short`。
- 证据：19 tests passed；3 skills valid；plugin validation passed；本机 `0.3.0+codex.20260820080751` installed/enabled；缓存规则和源码 `0.3.0` 已核验。

## 质量清单

- [x] 目标、范围、范围外、假设和成功标准明确。
- [x] 已检查架构、状态脚本、测试、技能、参考文档和 Git 基线。
- [x] 已考虑权限、并发写入、兼容性、恢复、失败降级和回滚。
- [x] 任务依赖、预期写入范围、验收标准和验证命令明确。
- [x] 提交和推送授权状态已单独记录；实施阶段未授权，交付后由用户明确授权。
- [x] 用户已确认本计划 revision 1。
- [x] 实施过程中一次只有一个任务为 `[~]`。
- [x] 保留上一轮未提交的入口准入规则修改。
- [x] 所有状态转换和 Worker 交付都有可验证证据。
- [x] 最终设计、计划、交付报告、Git 和安装状态一致。

## 确认记录

- 当前状态：revision 1 已确认并完成。
- 有效确认原文：`好，开始改造`。
- 计划确认后允许本地代码、测试、文档和本机插件缓存修改。
- 实施阶段未授权 Git 提交和推送；交付后用户通过 `帮我提交并推送到远端` 明确授权提交并推送到当前 `origin/main`。
- Tag、Release 和部署仍未授权。
